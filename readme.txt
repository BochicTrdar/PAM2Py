If a package is missing use pip to install it. 
Anaconda prompt (Windows): 
pip install soundfile
Linux commandline:
sudo pip3 install soundfile
Installation a la conda:
conda install -c bricew soundfile
